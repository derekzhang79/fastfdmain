#ifndef PUBLIC_PUSHBUTTON_NUMKEY_H
#define PUBLIC_PUSHBUTTON_NUMKEY_H

#include <QPushButton>

class public_pushbutton_numkey : public QPushButton
{
    Q_OBJECT
public:
    explicit public_pushbutton_numkey(QWidget *parent = 0);

signals:

public slots:
};

#endif // PUBLIC_PUSHBUTTON_NUMKEY_H
